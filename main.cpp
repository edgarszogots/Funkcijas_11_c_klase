#include <iostream>
using namespace std;
int process (int x, int y, int & max, double & avg) 
{
if (x>y){
  x = liel;
  else (x<y);
     y = liel;
       }
if ((x+y)/2,0) == x){
  x = vid;
}
if (x>y; x-y){
  y = diff;
  else (x<y; y-x)
  y = diff;
    }
}
int main() {
	int a,b,liel;
	double vid;
	cout << "Ievadiet skaitļus:\n";
	cin >> a >> b;
	int diff=process(a,b,liel,vid);
	cout << "Lielākais: "
		<< liel << endl;
	cout << "Vidējais: "
		<< vid	<< endl;
	cout << "Starpība: "
		<< diff << endl;
	return 0;
}
